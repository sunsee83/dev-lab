{
  "name": "reels-inspector",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs",
    "verify": "npm run build && npm run check && node --check ri-retry.user.js"
  },
  "devDependencies": {
    "esbuild": "0.25.9"
  }
}
