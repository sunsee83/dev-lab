{
  "name": "reels-inspector",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/unit/*.test.mjs",
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs",
    "verify": "npm run test && npm run build && npm run check && node --check ri-retry.user.js"
  },
  "devDependencies": {
    "esbuild": "0.25.9"
  }
}
